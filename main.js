const hamburger=document.querySelector('.hamburger');const menu=document.getElementById('menu');const hasSub=document.querySelector('.has-submenu');const toggle=document.querySelector('.submenu-toggle');if(hamburger&&menu){hamburger.addEventListener('click',()=>{const open=menu.classList.toggle('open');hamburger.setAttribute('aria-expanded',String(open));});}if(hasSub&&toggle){toggle.addEventListener('click',()=>{const open=hasSub.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}document.addEventListener('click',(e)=>{const btn=e.target.closest('[data-open]');if(!btn)return;const id=btn.getAttribute('data-open');const dialog=document.getElementById(id);if(dialog)dialog.showModal();});const toast=document.getElementById('toast');document.addEventListener('click',(e)=>{if(e.target.closest('[data-toast="add"]')){toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3000);}});